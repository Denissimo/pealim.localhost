old_sound_rabbit_mq:
    connections:
        default:
            url: '%env(RABBITMQ_URL)%'
    producers:
#        # use 'old_sound_rabbit_mq.task_producer' service to send data.
#        task:
#            connection:       default
#            exchange_options: { name: 'task', type: direct }

        # use 'old_sound_rabbit_mq.task_producer' service to send data.
        run_pealim_parse:
            connection:       default
            exchange_options: { name: 'run_pealim_parse', type: direct }
    consumers:
#        task:
#            connection:       default
#            exchange_options: { name: 'task', type: direct }
#            queue_options:    { name: 'task'}
#            callback:         App\Consumer\TaskConsumer
        run_pealim_parse:
            connection:       default
            exchange_options: { name: 'run_pealim_parse', type: direct }
            queue_options:    { name: 'run_pealim_parse'}
            callback:         App\Consumer\PealimParseConsumer
